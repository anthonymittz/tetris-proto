import colors from './static/colors.json';
import elements from './static/elements';

// window.addEventListener('keydown', e => {
//   for (var type in controls) {
//     controls[type].keys.forEach(key => e.code === key && controls[type].action());
//   }
// });
// window.addEventListener('beforeunload', e => console.log('[unload]'));
